
@{
}
@model Book

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>
            @Model.Title
            <span class="badge badge-pill badge-primary" style="float:right">
                <small>@Model.Price.ToString("c")</small>
            </span>
        </h4>
    </div>

    <div class="card-text p-1">
        <ul class="list-unstyled list-group-horizontal">
            <li >@Model.Author</li>
            <li>@Model.Category</li>
            <li>@Model.PageCount Pages</li>
            <li>@Model.Publisher</li>
            <li>@Model.Isbn</li>

        </ul>
    </div>

    <form method="post" asp-page="/BookSummary">
        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()"/>

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-right">Buy</button>
        </span>
    </form>
</div>
